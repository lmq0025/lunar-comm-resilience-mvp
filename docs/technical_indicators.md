# Technical Indicators

The MVP exports layered metrics for physical, network, and service layers, plus an acceptance table in `indicator_check.csv`.

Reference-only indicators such as RF lifetime prediction error, cascading fault prediction accuracy, and fault propagation delay error use built-in benchmark values in phase one.
